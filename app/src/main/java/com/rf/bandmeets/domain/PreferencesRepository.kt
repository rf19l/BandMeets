package com.rf.bandmeets.domain

import androidx.annotation.Nullable

interface PreferencesRepository {
    /*
    fun getPreferences(@Nullable fetchFresh: Boolean?): Observable<Preferences?>?

    fun savePreferences(preferences: Preferences?)

    fun clearPreferences()

    fun partialUpdate(preferences: Preferences?): Observable<Preferences?>?

    fun setPin(pin: String?): Observable<UseCaseResponse<Boolean?>?>?

     */
}